[build]
  command = "npm install"
  publish = "."

[functions]
  directory = "netlify/functions"

# Redirect all /api/* to unified api handler
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api"
  status = 200
  force = true
